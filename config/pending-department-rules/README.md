# Pending department access rules

The files in this directory are inactive holding records and are not inputs to the local project seeder.

Do not seed the `T2.0-STORE` rules until both conditions are true and verified with a live question from the real user:

1. Entra assigns `STORE_OPERATIONS` to a real user.
2. The backend mints `department:T2.0-STORE:STORE_OPERATIONS` for that user.
